{% extends 'OpenOrchestraDisplayBundle:Block:layout.html.twig' %}

{% block content %}
    <h3>
        {{ 'open_orchestra_newsletter.title.registration'|trans }}
    </h3>
    {% if confirmation_message is not null %}
        <div class="confirmation">
            {{ confirmation_message| trans }}
        </div>
    {% else %}
        {{ form(form) }}
    {% endif %}
{% endblock %}
